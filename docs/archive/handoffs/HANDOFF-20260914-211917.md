# rupi Agentic Development Handoff — 2026-09-14 21:19 UTC

Goal: `mu1n4gip-470v4y`
Goal tracker state: paused (tracker still reports 2/8 tasks complete; resume explicitly before the next substantive loop)
Stop reason: requested resumable handoff after the Phase 0–6 closeout; OpenAI usage is near the C1 threshold but has not reached it.
Branch: `agent/phase-2-trace-compression`
Remote: `origin/agent/phase-2-trace-compression`
Implementation HEAD: `27cd16c5a3db42ea373617c68a61212dc2987f9c` (`feat: add bounded package trust and MCP HTTP support`)
Handoff commit: `0fdd5fc` (`docs: add resumable development handoff`)

## Stop-condition status

The goal's ordered stop conditions are:

- **C1:** maximum OpenAI usage reaches at least 97%.
- **C2:** a worthy evidenced blocker or human-dependent decision requires stopping.
- **C3:** every Phase 0–11 task and stage gate is complete.

This handoff was written at the user's request. The latest usage snapshot is below C1, no new C2 blocker was identified, and Phases 7–11 remain incomplete; therefore this is a resumable development checkpoint, not a C1/C2/C3 completion claim.

## Usage status

Command:

```sh
codexbar --provider codex --json-only | jq '.[0].usage | {primary:.primary.usedPercent, secondary:.secondary.usedPercent, max:([.primary.usedPercent,.secondary.usedPercent]|max), primaryReset:.primary.resetDescription, secondaryReset:.secondary.resetDescription}'
```

Latest result:

- Primary/5-hour used: **96%**; reset: `6:16 PM`
- Secondary/weekly used: **53%**; reset: `Sep 19 at 10:00 AM`
- Maximum: **96%**; C1 threshold is **97%**

At the next loop start and before any substantial slice, re-run the command. If the maximum is at least 97%, finish only the immediate safe action and write a new dated handoff without beginning another implementation slice.

## Repository state

- The Phase 0–6 implementation and documentation are committed and pushed at `27cd16c`.
- The worktree has no modified tracked files.
- Untracked files are generated goal metadata only:
  - `.pi/.goals-pool-snapshot.json`
  - `.pi/goals/`
- Do not commit those `.pi` files unless the goal/task tooling explicitly requires it.
- No secrets or credentials were added.

## Completed work

### Phase 2: trace/provenance

- Added opt-in raw Deflate compression for content-addressed blob payloads only.
- Kept append-only `trace.jsonl` uncompressed.
- Preserved logical redacted hashes/sizes, backward-compatible raw `BlobRef` decoding, and explicit encoding/path metadata.
- Kept reconstructed rationale producer-free; hidden reasoning is never inferred.
- Propagated the store compression policy through `Store::blobs`.

### Phase 3: compatibility, local packages, and trust

- Added bounded `rupi packages install [--project] <local-directory>` for explicit local-directory copies only.
- Refused symlinked sources/destinations/package roots, unsafe manifest paths, path escapes, unsupported file types, overwrite, dependency installation, scripts, and network/package-manager work.
- Used regular-file copying with Unix `O_NOFOLLOW`, `create_new`, and `sync_all`; installation uses staging and atomic rename.
- Hardened package discovery and inspection against manifest path escapes.
- Added `FileTrustStore` with schema validation, fail-closed malformed/newer/duplicate handling, exact-scope replacement/removal, atomic writes, and 0700/0600 permissions.
- Added `Store::trust_store`, trust resolution, canonical project/git-root scopes, and `rupi trust --list/--grant/--deny/--clear` coverage.
- Denials override the explicit one-shot `--project` compatibility grant.
- Updated compatibility status and documentation: local installation is bounded and supported for this slice; npm/git/HTTP sources, updates, dependency installation, and extension execution remain deferred.

### Phase 6: MCP client

- Added lazy bounded Streamable HTTP transport with JSON responses and one bounded SSE data message.
- Added session propagation and negotiated `MCP-Protocol-Version` handling.
- Validated JSON-RPC version, IDs, result/error shape, status responses, reserved headers, and bounded request/response/pagination behavior.
- Redacted URL query/userinfo/token-like data in transport `Debug`, manager status, and configuration JSON; URL userinfo is rejected.
- Preserved lazy startup: HTTP construction performs no network I/O; servers activate explicitly through the manager/session path.
- Covered JSON/SSE, notification 202/204, sessions, status/ID failures, reserved headers, response limits, URL redaction, pagination limits, and lifecycle integration.
- Long-lived push, cancellation-aware network interruption, and broader MCP server/worker mode remain deferred.

## Verification evidence

The final Phase 0–6 verification completed successfully:

```text
cargo fmt --all --check                         PASS
cargo check -p rupi-core --all-features       PASS
cargo clippy --workspace --all-targets -- -D warnings  PASS
cargo test --workspace                          PASS
cargo doc --workspace --no-deps                 PASS
git diff --check                                PASS
```

Latest startup benchmark:

```sh
bash bench/startup.sh --json bench/results/startup-phase3-6.json
```

- Cold startup: **260.38 ms**
- Warm startup: min **2.63 ms**, mean **2.83 ms**, median **2.80 ms**, max **3.11 ms**

The benchmark is recorded in the working tree as an ignored/generated result; reproduce it when a later change affects startup or activation boundaries.

## Remaining roadmap work

### Phase 7 — `rkb-rs` first-party integration (next slice)

All Phase 7 items remain open:

- Create an independent `rupi-rkb` package/extension.
- Add RKB setup/discovery and an RKB skill.
- Add the RKB MCP connection path.
- Add citation-aware rendering.
- Define/implement `ExternalContextRef`.
- Preserve durable RKB resource IDs, source/citation metadata, and provenance.
- Compact inline evidence to a durable reference and rehydrate on demand.
- Emit external-context retrieval events.

Stage gate: RKB evidence must enter context, compact to references, rehydrate later, retain provenance across transformations, and keep `rkb-rs` independent with no core dependency.

Existing generic primitives to reuse/evaluate:

- `crates/rupi-core/src/context.rs::ExternalContextItem`
- `src/run.rs::turn_with_external_context`
- `AgentEvent::ExternalContextRetrieved`
- existing provenance and context-compaction/checkpoint contracts

The external repository to inspect read-only before designing the adapter is:

```text
/Users/saehwan/repos/rkb-rust
```

Do not infer its public API or add it as a `rupi-core` dependency without checking its actual contract.

### Phase 8 — TypeScript extension compatibility host

Unstarted. Define lazy Node RPC/lifecycle/isolation, selected tool/command/lifecycle/context/TUI APIs, and representative fixtures. Node must not launch when no extension requires it, and extension failure must not corrupt the core session.

### Phase 9 — MCP server / worker mode

Unstarted. Add coarse `agent.start/continue/cancel/branch/compact` operations and session/summary/message/trace/diff/artifact/checkpoint resources without terminal scraping or leaking unnecessary internals.

### Phase 10 — replay and research tooling

Unstarted. Add deterministic replay/filtering/context reconstruction, historical branching/comparison, timelines, provenance summaries, and export. Replay must remain distinct from new generation.

### Phase 11 — adaptive experiments

Unstarted. Only after stable baselines, measure context/backup/MCP trade-offs, keep static defaults stable, and require measurable benefit without reducing predictability.

### Deferred/ongoing work

- Remote package sources, dependency installation, package updates, and extension execution.
- Runtime/interactive durable trust prompting; current durable trust resolution is wired to compatibility/discovery CLI paths.
- Windows CI, telemetry/analytics, GitHub Pages, and broader upstream compatibility fixtures.
- Long-lived MCP push/cancellation and MCP server/worker mode.
- Ongoing performance, redaction, permissions, trust, subprocess, and compatibility audits.

## Residual risks and review boundaries

- Trust-store location must remain an explicit trusted input; never derive it from untrusted project content.
- Trust scope keys must remain canonical and exact; do not broaden a project grant to an ancestor or arbitrary path.
- Preserve fail-closed behavior for malformed/newer trust data and denial precedence.
- Keep package installation local-only and bounded unless a separate public contract explicitly authorizes network, dependency, script, or extension behavior.
- Keep MCP URL/header configuration redacted in all diagnostics and ensure project-local configuration is trust-gated before activation if that path is expanded.
- Do not turn generic external context into hidden-reasoning recovery or a mandatory startup dependency.
- Preserve `Unknown` for uncertain mutating tool effects and keep native reasoning, provider summaries, declared rationale, and reconstructed rationale distinct.
- Keep one active model in normal execution; backup activation remains fault recovery, not orchestration.
- Re-run startup and relevant render/restore benchmarks whenever Phase 7+ changes activation or context reconstruction.

## Exact next actions

1. Explicitly resume goal `mu1n4gip-470v4y` before substantive work; the tracker is still paused administratively.
2. Run the usage query above. If maximum usage is at least 97%, stop and write only a fresh C1 handoff.
3. Read the current canonical design, architecture, compatibility, roadmap, harness, and invariant-review guidance before the Phase 7 design.
4. Inspect `/Users/saehwan/repos/rkb-rust` and determine its setup/discovery, MCP, resource-ID, and citation contracts without guessing.
5. Create a bounded Phase 7 change brief and independent `rupi-rkb` boundary. Keep `rupi-core` free of an `rkb-rs` dependency.
6. Design tests first for citation/provenance preservation, inline-to-reference compaction, durable reference storage, on-demand rehydration, retrieval events, and unavailable-resource failure behavior.
7. Implement the smallest complete RKB adapter slice, update only evidence-backed roadmap/design/compatibility docs, and push a small commit.
8. Run focused tests, invariant review, workspace verification, and startup/restore benchmarks implicated by the slice.
9. At each loop boundary, re-check usage and write another dated handoff at C1, C2, or the next user-requested pause.
