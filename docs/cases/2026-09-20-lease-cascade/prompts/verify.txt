Perform a read-only verification of the Lease Cascade project in the current
workspace. Do not edit or write any file and do not use the acceptance oracle as
an implementation import. Run the project unittest suite, top-level/serve/
worker help commands, a standard-library import review, and a concise smoke
check if the project is complete. The verify config permits `exec`, but use it
only for these read-only commands; use direct process argv for Python and avoid
shell writes. Exclude `.rupi-state*`, `__pycache__`, and the acceptance
directory from broad file listings and import review. Report exact commands,
exit statuses, and whether the project appears ready for the independent
fresh-process oracle.
