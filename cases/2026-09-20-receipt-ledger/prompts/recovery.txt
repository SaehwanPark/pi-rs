Continue the incomplete Receipt Ledger implementation in this workspace.
Read SPEC.md and inspect the files already present. Work only in the project
workspace and do not edit the acceptance oracle. Finish missing implementation,
README, and focused tests. Prioritize exact HMAC admission and SQLite restart
persistence, direct-argv delivery, ordered fan-out/barrier fan-in, leases and
reclaim, conditional token-matched finalization, stale-worker non-overwrite,
and lost-acknowledgement recovery: the same stable delivery key must let an
idempotent sink replay one durable receipt without applying the logical side
effect twice.

The new capstone contract is the SQLite append-only audit ledger: canonical
event JSON, contiguous sequence, prev_hash/event_hash SHA-256 chain, atomic
state-plus-audit commits, safe fields only, and a read-only audit --verify
command that detects tampering. Do not weaken this contract or claim that audit
records execute work. Use direct argv on Windows. Run the complete project
unittest suite and audit verifier before giving a final summary. If anything
remains incomplete, say exactly what failed instead of claiming success.
